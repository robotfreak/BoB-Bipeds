<UserControl x:Class="ServoControl.GroupDisplayPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border Background="LightGray" CornerRadius="15" BorderBrush="Gray" BorderThickness="2" Margin="5">
            <Grid>
                <Border VerticalAlignment="Top" Height="30" CornerRadius="15,15,0,0" Name="border">
                    <Border.Background>
                        <LinearGradientBrush  >
                            <GradientStop Color="Blue" Offset="0"></GradientStop>
                            <GradientStop Color="Transparent" Offset="1"></GradientStop>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
                <Label FontWeight="Bold" FontSize="18" Margin="5,-5,0,0" Foreground="white" Name="titleLabel">Title</Label>
                <Grid Name="childGrid" Margin="5,30,5,5">
                    
                </Grid>
            </Grid>
            <Border.Effect>
                <DropShadowEffect></DropShadowEffect>
            </Border.Effect>
        </Border>            
    </Grid>
</UserControl>
